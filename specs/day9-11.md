# Day 9~11 — 앱 배포·관측·무중단 증명

## Day 9 DoD
- `helm/docmind`, `helm/flashgate` 차트: deployment(otel agent initContainer로 jar 복사), service, ingress(TLS), hpa, pdb, configmap. `helm template` 통과, `scripts/apply.sh` 로 배포
- 의존 인프라: pgvector StatefulSet(docmind ns), redis sentinel(bitnami chart, sentinel enabled, replica 2), kafka(bitnami KRaft 1노드), mysql(bitnami) — 전부 requests/limits 지정
- Grafana Explore: flashgate `POST /api/orders` 트레이스에 Redis·MySQL·Kafka 스팬이 보이고, trace_id로 로그 점프
- SealedSecret: `flashgate-secrets`, `docmind-secrets`(OPENAI_API_KEY)

## Day 10 DoD
- `scripts/rollout-test.sh` 실행 → `failed_rate=0` 결과를 `docs/rollout-YYYYMMDD.md`에 기록. 0이 아니면 preStop sleep 5s / readiness 주기 조정 후 재측정(조정 근거 문서화)
- 대시보드 2개 JSON을 `docs/dashboards/`에: `red.json`(RPS·에러율·p50/p99 per service), `jvm.json`(heap, GC, threads). Grafana provisioning ConfigMap으로 로드
- Alertmanager 규칙 3개: 5xx 비율 > 1% 5분, flashgate_stock_mismatch == 1, Pod not ready 5분

## Day 11 DoD
- 세 URL HTTPS 200, docmind public 모드 응답, flashgate k6 spike 결과(서버 기준) 1회 기록
- `scripts/backup.sh`: etcd snapshot + mysql/pg dump → 서버 로컬 + (선택) 외부 스토리지, cron 등록
- `docs/runbook.md` 실제 겪은 장애로 갱신, README 아키텍처 그림 확정
